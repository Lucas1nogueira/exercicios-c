#include <stdio.h>

int main() {
	int A, B, C, soma;

	printf("Insira o valor de A:\n");
	scanf("%d", &A);

	printf("Insira o valor de B:\n");
	scanf("%d", &B);

	printf("Insira o valor de C:\n");
	scanf("%d", &C);

	printf("O resultado da soma de A, B e C e igual a: %d", soma = (A*A) + (B*B) + (C*C));

return 0;
}
